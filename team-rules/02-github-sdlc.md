# GitHub SDLC (glimpsovstar)

For features, bugs, and deploy errors (not docs-only): GitHub issue → branch feature/<#>-slug or fix/<#>-slug → implement with tests/docs → verify → PR with Fixes #N → close on merge.

Skip only when user explicitly skips SDLC.
