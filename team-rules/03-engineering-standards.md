# Engineering standards (glimpsovstar)

Ask 1-2 focused questions when scope is unclear. Verify before claiming done (run tests/build; evidence before assertions). Minimal diffs; match repo conventions. Never commit secrets.
